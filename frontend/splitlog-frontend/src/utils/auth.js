/**
 * @deprecated
 * Use `useAuth()` from `context/AuthContext` instead.
 * Auth state is now managed exclusively via HTTP-only cookies — no localStorage.
 */

export const getUser = () => null;
export const isLoggedIn = () => false;
